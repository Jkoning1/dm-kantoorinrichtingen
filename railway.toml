[build]
  builder = "NIXPACKS"
  buildCommand = "bash build.sh"

[deploy]
  startCommand = "cd cms && PAYLOAD_CONFIG_PATH=dist/payload.config.js node dist/server.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
