[build]
  builder = "NIXPACKS"
  buildCommand = "bash build.sh"

[deploy]
  startCommand = "PAYLOAD_CONFIG_PATH=cms/dist/payload.config.js node cms/dist/server.js"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
